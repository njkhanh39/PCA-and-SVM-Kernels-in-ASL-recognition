# PCA-and-SVM-Kernels-in-ASL-recognition
Explores and attempts to address the task by comparing four approaches of Support Vector Machines (SVM) to detect the American Sign Language (ASL) alphabet: by using Radial Basis Function (RBF) Kernel and Polynomial Kernel, in which each method is experimented both separately and paired with Principal Component Analysis (PCA)
